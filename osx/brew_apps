# vim: ft=sh

set -uex

brew install \
    git cmake cscope ctags apktool  \
    lftp nmap aria2 p7zip pv screen \
    unrar xz wget midnight-commander \
    dos2unix htop-osx

if false ; then
    # removed and using
    # using version in osx:
    ruby screen imagemagick privoxy
    zsh
    macvim --override-system-vim
fi
